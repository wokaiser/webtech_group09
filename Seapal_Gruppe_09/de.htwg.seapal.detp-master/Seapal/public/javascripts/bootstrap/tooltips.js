$(function() {
	
	$(".viewTooltip").tooltip({
        'selector': '',
        'placement': 'top',
         delay: { show: 500, hide: 100 }
    });
	
	$(".modifyTooltip").tooltip({
        'selector': '',
        'placement': 'top',
         delay: { show: 500, hide: 100 }
    });
    
    $(".deleteTooltip").tooltip({
        'selector': '',
        'placement': 'top',
         delay: { show: 500, hide: 100 }
    });
	
});